# Demo

Run `python scripts/seed_demo_data.py` to create explicitly synthetic Wi-Fi data including an upstream-like degradation. Start the API and dashboard, then refresh the page. `python scripts/run_demo_scenario.py --scenario packet_loss` is safe: it only writes generated telemetry to the local development database.
