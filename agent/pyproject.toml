[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="netsentinel-agent"
version="0.1.0"
dependencies=["psutil>=6", "httpx>=0.27", "pydantic>=2.7", "dnspython>=2.6", "PyYAML>=6"]
[tool.setuptools.packages.find]
where=["src"]
